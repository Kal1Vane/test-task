
import { Typography } from '@mui/material';
import styled from 'styled-components';

// STYLED TYPOGRAPHY


export const NormalText = styled(Typography)`
   && {
     font-family: Roboto, sans-serif;
     font-size:16px;
     font-weight: 400;
     color: #575756;
   }
`;
 
 
export const Normal14Text = styled(Typography)`
   && {
     font-family:Roboto, sans-serif;
     font-size:14px;
     font-weight: 400;
     color: #575756;
   }
`;